// Programa correto. Somente variáveis já declaradas são usadas.
let
    integer foo, bar, boo.
in
    if (foo = bar) then
        // abc := 3;
        foo := bar * 33 + 22 / 2 - boo;
    else
    fi;
end